# ADR-002: SQLite

## Decision

Use SQLite (`better-sqlite3` + Drizzle) with a checked-in seed script producing `data/demo.sqlite`.

## Rationale

One-command local demo; deterministic data; no cloud warehouse required.
