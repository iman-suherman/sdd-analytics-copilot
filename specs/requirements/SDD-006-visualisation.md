# SDD-006 Visualisation

## Requirement

For each analytics result the system SHALL recommend a visualisation (`kpi` | `line` | `bar` | `table`).

The UI SHALL render charts from result data (Apache ECharts) alongside textual answers and evidence.
