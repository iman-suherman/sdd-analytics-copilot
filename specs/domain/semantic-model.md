# Domain: Semantic model

See `semantic/commerce.yaml` (runtime) and `SDD-003`.

The semantic model is the **only** vocabulary the agent may use for metrics and dimensions. It maps business concepts to physical fields and metric filters (e.g. `orders.status = completed`).
